	version: 2.1
orbs:
 node: circleci/node@1.1.6
jobs:
 build: 
  exeutor:
   name: node/default
  steps: 
   - checkout
   - node/with-cache:
      steps:
       - run: npm install

workflows:
 build-and-test:
  jobs:
       - build 



